// Generated by CoffeeScript 1.8.0
var BrowserSyncTask, browserSync;

browserSync = require('browser-sync');

BrowserSyncTask = (function() {
  function BrowserSyncTask(gulp, config) {
    this.gulp = gulp;
    this.config = config;
    if (this.config === null) {
      this.config = {
        browser: 'chrome',
        logPrefix: 'Task Runner - tests',
        notify: true,
        open: true,
        port: 3000,
        server: {
          baseDir: '/',
          index: 'index.html'
        }
      };
    }
  }

  BrowserSyncTask.prototype.setupTask = function() {
    return browserSync(this.config);
  };

  return BrowserSyncTask;

})();

if ((typeof module !== "undefined" && module !== null) && (module.exports != null)) {
  module.exports = BrowserSyncTask;
}
